<Window x:Class="NobelLaureates.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NobelLaureates"
        xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="Nobel Laureates" 
        Height="350" Width="525"
        DataContext="{Binding Shell, Source={StaticResource bootstrapper}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <xcdg:DataGridControl Grid.Row="1" 
                              DataContext="{Binding GridViewModel}"
                              ItemsSource="{Binding Rows.Value}">
            
        </xcdg:DataGridControl>
        
        
    </Grid>
</Window>
